apiVersion: storage.k8s.io/v1
kind: CSIDriver
metadata:
  name: {{ include "hf-csi.driverName" . }}
  labels:
    {{- include "hf-csi.labels" . | nindent 4 }}
spec:
  attachRequired: false
  podInfoOnMount: true
  volumeLifecycleModes:
    - Ephemeral
    - Persistent
  fsGroupPolicy: File
